import { ViteDevServer } from "vite";
export default function dependencyWatcher(dependenciesPath?: string[], dependenciesName?: string[]): {
    name: string;
    apply: string;
    configureServer(server: ViteDevServer): void;
    config(): {
        optimizeDeps: {
            exclude: string[];
        };
    };
};
